{
  "name": "symmetry",
  "version": "0.1.0",
  "private": true,
  "description": "A gamified, AI-ready glow-up and lock-in tracker.",
  "type": "module",
  "scripts": {
    "dev": "npx serve www",
    "build": "node -e \"console.log('Symmetry is a static app; deploy the www/ directory.')\"",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "apk:debug": "npm run cap:sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0",
    "serve": "^14.2.4"
  }
}
